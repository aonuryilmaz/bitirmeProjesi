import { StyleSheet } from "react-native";

const Styles=StyleSheet.create({
    container:{
        flex:1,
        marginTop:5
    },
    saveButton:{
        marginTop:5,
        marginLeft:4,
        marginRight:4
    },
    labels:{
        fontWeight:"bold"
    }

})

export default Styles;